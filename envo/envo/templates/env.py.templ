from dataclasses import dataclass

from {{ package_name }}.env_comm import {{ class_name }}Comm


@dataclass
class {{ class_name }}({{ class_name }}Comm):
    class Meta({{ class_name }}Comm.Meta):
        stage = "{{ stage }}"
        emoji = "{{ emoji }}"

    def __init__(self) -> None:
        super().__init__()


Env = {{ class_name }}

